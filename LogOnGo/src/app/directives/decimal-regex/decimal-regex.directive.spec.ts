import { DecimalRegexDirective } from './decimal-regex.directive';

describe('DecimalRegexDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalRegexDirective();
    expect(directive).toBeTruthy();
  });
});
